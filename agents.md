# ReviewG33k Notes

Future enhancements:

1. Add an option that allows Codex to fix one or more warnings reported in the log.
2. Present warnings/errors in a dedicated review window with a per-item `Fix` button.
3. Allow double-clicking a log line to open the file at the correct line in VS Code (when VS Code is detected).
